server:
  adminConnectors:
    - type: http
      port: 8181
  applicationConnectors:
    - type: http
      port: 8180
database:
  driverClass: org.hsqldb.jdbc.JDBCDriver
  user: sa
  password: =sa
  url: jdbc:hsqldb:file:/Users/tsharpe/git/genomebridge-boss/boss-ws/target/test-classes/testdb;shutdown=true
  validationQuery: SELECT * FROM INFORMATION_SCHEMA.SYSTEM_TABLES
cloudStore:
  username: serviceAccountEmail
  password: key.p12
  bucket: someBucket
  endpoint: https://storage.googleapis.com
  type: GCS
localStore:
  username: s3User
  password: bogus
  bucket: someBucket
  endpoint: s3Server
  pathStyleAccess: true
  type: S3
