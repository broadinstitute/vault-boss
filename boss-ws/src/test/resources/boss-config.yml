server:
  adminConnectors:
    - type: http
      port: 8181
  applicationConnectors:
    - type: http
      port: 8180
database:
  driverClass: ${driver}
  user: ${username}
  password: ${password}
  url: ${url}
  validationQuery: ${validationQuery}
objectStore:
  bucket: genomebridge-variantstore-ci
