server:
  adminConnectors:
    - type: http
      port: 8181
  applicationConnectors:
    - type: http
      port: 8180
database:
  driverClass: ${dataSource.driver}
  user: ${dataSource.username}
  password: ${dataSource.password}
  url: ${dataSource.url}
  validationQuery: ${dataSource.validationQuery}
cloudStore:
  username: ignoredInUnitTesting
  password: ignoredInUnitTesting
  bucket: bossbucket
  endpoint: http://localhost:8180/fcs
  type: FCS
localStore:
  username: ecsUserName
  password: ecsPassword
  bucket: bossbucket
  endpoint: https://ecsserver.broadinstitute.org
  pathStyleAccess: true
  type: S3
