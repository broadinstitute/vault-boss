server:
  adminConnectors:
    - type: http
      port: 8181
  applicationConnectors:
    - type: http
      port: 8180
database:
  driverClass: com.mysql.jdbc.Driver
  user: egxapp
  password: password
  url: jdbc:mysql://localhost:3306/testdb
  validationQuery: SELECT * FROM objects
objectStores:
  cloudStore:
    username: ignoredInUnitTesting
    password: ignoredInUnitTesting
    bucket: bossbucket
    endpoint: http://localhost:8180/fcs
    type: FCS
    readOnly: false
  localStore:
    username: ecsUserName
    password: ecsPassword
    bucket: bossbucket
    endpoint: https://ecsserver.broadinstitute.org
    pathStyleAccess: true
    type: S3
    readOnly: false